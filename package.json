{
    "name": "glide",
    "version": "1.0.0",
    "license": "MIT",
    "private": true,
    "type": "module",
    "workspaces": [
        "apps/*",
        "libs/*"
    ],
    "scripts": {
        "dev:server": "node scripts/dev-server-wrapper.cjs",
        "dev:server:raw": "nx serve server-electron",
        "dev:client": "nx serve client-pwa",
        "build:server": "nx build server-electron",
        "build:client": "nx build client-pwa",
        "build:all": "npm run build:client && npm run build:server",
        "dist:win": "npm run build:all && cd apps/server-electron && npm run dist",
        "dist:mac": "npm run build:all && cd apps/server-electron && npm run dist:mac",
        "setup:autostart": "node scripts/setup-autostart.cjs enable",
        "remove:autostart": "node scripts/setup-autostart.cjs disable",
        "setup:firewall": "node scripts/setup-firewall.cjs open",
        "remove:firewall": "node scripts/setup-firewall.cjs close"
    },
    "devDependencies": {
        "@nx/react": "^20.3.0",
        "@nx/vite": "^20.3.0",
        "@nx/workspace": "^20.3.0",
        "nx": "^20.3.0",
        "typescript": "^5.8.3"
    }
}