services:
  - type: web
    name: glide-signaling
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build:signaling
    startCommand: node apps/signaling/dist/src/main.js
    healthCheckPath: /health
